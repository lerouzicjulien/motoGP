import { Typography } from "@mui/material";

const Header = () => {
    return(
        <div className="header">
            <Typography variant="h1" sx={{margin: 3 }}>Moto Gp riders and teams</Typography>
        </div>
    )
};

export default Header;